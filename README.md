# Everfracted Local Preview

This is a static HTML site. To run it locally:

```sh
cd "/Volumes/FLORAL BIZ/Everfracted"
python3 -m http.server 8000
```

Open `http://localhost:8000/` in your browser.

## Hosting prep

Update `sitemap.xml` and `robots.txt` to use your real domain before publishing.
